# CORE PRODUCTION DEPENDENCIES - GUARANTEED TO WORK
# This file contains only essential dependencies that are stable across Python versions

# Core FastAPI and ASGI server
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Data validation and settings
pydantic==2.11.7
pydantic-settings==2.3.4

# Authentication and security
PyJWT[crypto]==2.10.1
cryptography==45.0.5
email-validator==2.2.0
passlib[bcrypt]==1.7.4
bleach==6.2.0

# Core APIs and HTTP
# OpenAI removed - migrated to Anthropic Claude 3.5 Sonnet
supabase==2.17.0
httpx==0.28.1
requests==2.32.4
urllib3==2.5.0
elevenlabs>=1.0.0  # Premium TTS for PAM voice

# Database essentials
redis==6.2.0
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Basic utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
python-multipart>=0.0.10
click==8.1.7

# Lightweight TTS that actually works
edge-tts==6.2.1  # Updated to fix Microsoft 403 authentication errors

# Essential monitoring
structlog==25.4.0
python-json-logger==3.3.0
sentry-sdk[fastapi]==2.33.0

# CORS handling
fastapi-cors==0.0.6

# LangChain and LangGraph for agent architecture
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0  # Agent orchestration framework
langchain-openai>=0.2.0  # OpenAI integration for LangGraph

# Basic task scheduling
APScheduler==3.11.0

# Essential processing
numpy==2.1.3  # Python 3.13 compatible (has precompiled wheels)
Pillow==11.3.0

# YouTube integration
youtube-transcript-api==0.6.2
google-api-python-client==2.149.0

# Task queue and background jobs
celery==5.3.4
kombu==5.3.4

# HTTP and web scraping
aiohttp>=3.12.14  # Security fix: CVE-2025-53643
beautifulsoup4==4.13.4

# Audio and voice processing (optional)
# soundfile==0.12.1  # Causes issues on Render
# pyttsx3==2.99  # Platform-specific
assemblyai==0.42.0

# Configuration and data processing
pyyaml==6.0.2
tiktoken>=0.8.0
textstat==0.7.7

# System monitoring and metrics
psutil==6.0.0
prometheus-client==0.22.1

# WebSocket support (13.1 required for websockets.asyncio module)
websockets==13.1

# Payment processing
stripe==12.3.0

# FastAPI dependencies (managed by FastAPI itself)
# starlette - DO NOT SPECIFY VERSION, FastAPI manages this
