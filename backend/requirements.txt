# RESTORED PRODUCTION DEPENDENCIES - FROM LAST KNOWN WORKING STATE
# These versions were confirmed working before the breaking changes

# Core FastAPI and ASGI server
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Data validation and settings
pydantic==2.11.7
pydantic-settings==2.3.4

# Authentication and security
PyJWT[crypto]==2.10.1
cryptography==45.0.5
email-validator==2.2.0
passlib[bcrypt]==1.7.4

# Core APIs and HTTP
openai>=1.50.0,<2.0.0
supabase==2.17.0
httpx==0.28.1
requests==2.32.4
urllib3==2.5.0

# Database essentials
redis==6.2.0
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0

# Basic utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
python-multipart>=0.0.10
click==8.1.7

# TTS Engines for high-quality voice synthesis
edge-tts==6.1.9                    # Microsoft Edge TTS (primary)
pyttsx3>=2.90                      # System TTS fallback
soundfile>=0.12.1                  # Audio processing for TTS
# TTS>=0.22.0                      # Coqui TTS (requires Python 3.11, causes Docker build issues)
# torch>=2.0.0                     # Required for Coqui TTS neural models  
# torchaudio>=2.0.0               # Audio processing for neural TTS

# Essential monitoring
structlog==25.4.0
python-json-logger==3.3.0
sentry-sdk[fastapi]==2.33.0

# CORS handling
fastapi-cors==0.0.6

# Basic LangChain (core only - no extras that break)
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# Basic task scheduling
APScheduler==3.11.0

# Essential processing
numpy==1.26.4
Pillow==11.3.0

# System monitoring (from stable version)
psutil==7.0.0
prometheus-client==0.22.1

# Additional stable dependencies
stripe==12.3.0
beautifulsoup4==4.13.4
tiktoken>=0.8.0
assemblyai==0.42.0

# OpenTelemetry for observability (stable versions)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0

# AI Agent Observability (stable versions)
langfuse>=2.0.0
agentops>=0.4.15,<0.5.0

# Graph Database (Optional - for Graph RAG)
neo4j>=5.14.0,<6.0.0

# Text analysis and TTS
textstat==0.7.7


# Local Whisper removed for memory optimization (eliminates 72MB+ model downloads)
# Using cloud-based OpenAI Whisper API instead (already included in openai package)

# Search and web processing
googlemaps==4.10.0
lxml==5.2.1

# YouTube scraping
youtube-transcript-api==0.6.2
google-api-python-client==2.111.0
google-auth==2.25.2
google-auth-httplib2==0.2.0

chromadb==0.4.22
sentence-transformers==2.7.0
whisper==1.1.1