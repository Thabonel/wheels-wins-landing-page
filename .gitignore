node_modules/
dist/
build/
.git.corrupted.backup/
*.log
.DS_Store
__pycache__/
*.pyc
.pytest_cache/
coverage/
.env
.env.local
.env.production
.env.staging
.env.ai-agents.local

# Claude Code local session memory (personal notes, not for git)
CLAUDE.local.md
*.local.md

# Package lock files (prevent accidental dependency updates)
package-lock.json
yarn.lock

# Git Safety Systems - Generated Files (DO NOT COMMIT)
.git/git-safe/
.git/git-network-resilient/
.git/git-health-monitor/
.git/git-script-auditor/
*.git-safe.backup
REPO_HEALTH_CHECK.md
scripts/git-safe-config
git-safety-functions.sh

# Knip Dead Code Analysis (Development Only - DO NOT COMMIT)
knip-*.json
knip-scan.json
knip-initial-scan.json
.knip-cache/

# 🔒 Security & Secrets Prevention (DO NOT COMMIT)
# Environment files with potential secrets
*.env*
.env
.env.*
.environment
*.secrets
*secret.*
*secret_*
*_secret*
*credential*
*password*
*private*

# Exception: Allow documentation files
!*PREVENTION*

# API Keys and tokens
*api-key*
*api_key*
*token*
*jwt*

# Scripts with database connections
*execute*.sh
*db-connect*.sh
*pgpassword*.sh

# Temporary files that may contain secrets
docs/maintenance/*.txt
*.backup.sql
*.dump.sql
*apikey*
*access-token*
*access_token*
*auth-token*
*auth_token*
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# Database and service credentials
database.yml
config/database.yml
*connection-string*
*database-url*
*db-url*
*db_url*

# Cloud provider credentials
*aws-credentials*
*gcp-credentials*
*azure-credentials*
.aws/credentials
.azure/credentials
service-account*.json
gcloud-service-key*.json

# Secret scanning tools (local development only)
.secrets.baseline
.gitleaks.toml
.detect-secrets-baseline

# Local pre-commit cache (don't deploy to Netlify)
.pre-commit/

# Console logs and debug output (may contain secrets)
docs/maintenance/*.txt
docs/maintenance/*.log
*console*.txt
*console*.log
*debug*.txt
*debug*.log

# Backup files that might contain secrets
*.backup
*.bak
*.old
*_backup
*_bak
backups/
test_*.py
*_test_results_*.json
orchestrator_test_*.json

# AI Migration Documentation (may contain references to keys)
GEMINI_*.md
ONGOING_MONITORING_*.md
PAM_AI_*.md
# E2E test reports (generated)
e2e/reports/*.json

# Beads Issue Tracker (local only)
.beads/*.db
.beads/*.db-journal
.beads/*.db-wal
.beads/*.db-shm
.beads/*.jsonl

# MCP Server Configuration (contains service role keys - DO NOT COMMIT)
.mcp.json

# MCP configuration with secrets
.claude.json
scripts/import-mcp-servers.sh
