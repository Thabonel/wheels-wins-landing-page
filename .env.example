# Example environment configuration
# Copy this file to .env and fill in your own values

# Supabase configuration
# Client-side credentials for the Supabase project
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=dev-anon-key

# Optional service role key used by backend scripts
SUPABASE_SERVICE_ROLE_KEY=dev-service-role-key

# Backend API Configuration
# For production: use https://pam-backend.onrender.com
# For local development: use http://localhost:8000
VITE_BACKEND_URL=http://localhost:8000

# WebSocket endpoint for PAM
# For production: use wss://pam-backend.onrender.com/api/v1/pam/ws  
# For local development: use ws://localhost:8000/api/v1/pam/ws
VITE_PAM_WEBSOCKET_URL=ws://localhost:8000/api/v1/pam/ws

# n8n integration discontinued - removed VITE_N8N_TRIP_WEBHOOK

# Mapbox Token Configuration (Standardized)
# Frontend public token for map rendering (URL-restricted, read-only scopes)
VITE_MAPBOX_TOKEN=pk.your_public_mapbox_token_here

# Backend secret token for API operations (server-only, full permissions)  
# MAPBOX_SECRET_TOKEN=sk.your_secret_mapbox_token_here

# YouTube Data API v3 key (for PAM YouTube Trip Scraper)
VITE_YOUTUBE_API_KEY=your_youtube_data_api_v3_key_here

# Default timeouts (in seconds)
HTTP_TIMEOUT=10
VITE_FETCH_TIMEOUT=10000

# Sentry Error Monitoring
VITE_SENTRY_DSN=https://your-sentry-dsn-here@o0000000000000000.ingest.de.sentry.io/0000000000000000
VITE_SENTRY_ENVIRONMENT=development

# AI Provider Configuration
# Primary: Google Gemini Flash API key (fastest and most cost-effective)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Fallback: Anthropic Claude API Configuration
# API key for Claude AI integration (fallback for PAM component)
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Other environment variables can go here
