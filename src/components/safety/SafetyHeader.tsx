
import { Link } from "react-router-dom";
import { Button } from "@/components/ui/button";

// This component is kept for backwards compatibility but now returns null
const SafetyHeader = () => {
  return null;
};

export default SafetyHeader;
